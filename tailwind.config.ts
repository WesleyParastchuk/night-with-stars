import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        ballet: ["Ballet", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
